- defaults: !defaults windows

- shell: !submodule .dotfiles
    .ssh

- link: !link alacritty
    git
    nvim
    ssh

- shell: !ps install_chocolatey
    install_chocolatey_packages
    install_winget_packages
