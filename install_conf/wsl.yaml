- defaults: !defaults ubuntu

- shell: !submodule .dotfiles
    etc
    .ssh

- clean: ["~"]

- create:
    - ~/.config
    - ~/.ssh
    - /etc/fonts
    - /etc/ssh

- link: !link alacritty
    dein
    fonts
    git
    nvim
    ssh
    ssh_config
    sshd_config
    starship
    tmux
    zsh

- shell: !shell replace_git_latest
    apt_update_upgrade

- apt: !apt wsl

- shell: !shell update_locale
    zsh_default_shell
    install_zinit

- shell: !shell install_asdf
    link_fonts
    install_sdkman
    install_act
    install_awscli
    install_bat
    install_chromedriver
    install_direnv
    install_fzf
    install_gcloud
    install_gh
    install_ghq
    install_go
    install_helm
    install_jq
    install_kubectl
    install_neovim
    install_node
    install_poetry
    install_pyenv
    install_ruby
    install_ripgrep
    install_rust
    install_starship
    install_terraform
    install_tmux
    install_zoxide
    install_xq
    install_postgres
    prepare_android
