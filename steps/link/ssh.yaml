~/.ssh/keys:
  path: links/.ssh/keys
  create: true
  relink: true
~/.ssh/config:
  path: links/.ssh/config
  create: true
  relink: true
~/.ssh/conf.d:
  path: links/.ssh/conf.d
  create: true
  relink: true
